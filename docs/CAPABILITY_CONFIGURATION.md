# Capability Configuration

Configuration is passed alongside the manifest. It splits variables into:
- `static_config`: Hardcoded system defaults.
- `runtime_config`: Mission-specific context passed by graph.
- `environment_config`: Secret variables mapped securely.
